
// load common configurations to undo class specific ones
exec common

// load engineer fast building
exec fastbuilding

// load 3slot pms order mousewheel for wrangler safety
exec mwheel_3slot_pms

// FIXME: this doesn't work yet, load order problems
// bind 0 in case we aren't using wrangler we can get normal mwheel
//bind "0" "exec mwheel_3slot"
// bind 9 so we can turn swap the weapon order for wrangler
//bind "9" "exec mwheel_3slot_pms"

echo "==Engineer class config loaded=="
//echo "Use 0 to set weapon select for m